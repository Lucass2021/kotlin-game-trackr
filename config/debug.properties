# Default API base URL for debug builds.
# Android Emulator reaches the host machine through 10.0.2.2 (not localhost).
# Override per-machine in config/local.properties (gitignored) — e.g. a physical
# device pointing at your machine's LAN IP.
API_BASE_URL=http://10.0.2.2:8000/api
